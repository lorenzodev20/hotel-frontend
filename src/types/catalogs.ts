export type Accommodation = {
    id: number,
    name: string
}

export type RoomTypes = {
    id: number,
    name: string
}


export type RoomType = {
    id: number,
    name: string
}

export type StateOrCity = {
    id: number,
    name: string
}